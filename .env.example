# Backend
PORT=3001
DATABASE_PATH=data/rezeptflow.db
MOCK_ZOHO_PATH=data/mock-zoho
REMINDER_HOURS=8
ESCALATION_HOURS=24
DELIVERY_TARGET_HOURS=48
ORDER_WEBHOOK_SECRET=demo-secret

# OpenCode Zen (optional — fixture fallback when missing)
OPENCODE_ZEN_API_KEY=
OPENCODE_ZEN_BASE_URL=https://opencode.ai/zen/v1
OPENCODE_ZEN_MODEL=deepseek-v4-flash-free

# Frontend (Vite)
VITE_API_URL=http://localhost:3001

# n8n (optional profile)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin
WEBHOOK_URL=http://backend:3001
