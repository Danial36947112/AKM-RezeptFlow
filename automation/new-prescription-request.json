{
  "name": "New Prescription Request",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "prescription-requested",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "prescription-requested"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.WEBHOOK_URL || 'http://backend:3001'}}/webhooks/prescription-requests",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ sourceEventId: $json.body.sourceEventId || $json.body.source_event_id, patientRef: $json.body.patientRef, physicianRef: $json.body.physicianRef, deliveryRef: $json.body.deliveryRef, materialRef: $json.body.materialRef, fieldEmployeeRef: $json.body.fieldEmployeeRef, idempotencyKey: $json.body.idempotencyKey }) }}"
      },
      "id": "http",
      "name": "POST Workflow API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [480, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [[{ "node": "POST Workflow API", "type": "main", "index": 0 }]]
    }
  },
  "settings": { "executionOrder": "v1" },
  "meta": {
    "description": "Normalize prescription webhook and forward to RezeptFlow Node API. Demo path can call API directly."
  }
}
